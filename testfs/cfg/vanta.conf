# Vanta OS Configuration

# Shell settings
shell=/apps/mt-shell
prompt=vanta>

# User settings
default_user=mystyy
home=/users/mystyy

# System paths
apps_path=/apps
temp_path=/temp
cfg_path=/cfg

# Kernel settings
kernel=/core/kernel.bin
bootloader=/core/boot.bin

# Device paths
console=/dev/console
null=/dev/null